function sum(first, second){
    let result = first + second + bonus;
    //sonsole.log(bonus);

    if(result > 9){
        var mood = "happy";
        mood = "fishy";
        mood = "cranky"
        //console.log(mood);
    }
    console.log(day);
    let day = "friday";

    return result;
}
